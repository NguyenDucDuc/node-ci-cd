name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Connect to EC2
        run: ssh -i "ubuntu_key.pem" ubuntu@ec2-54-169-229-203.ap-southeast-1.compute.amazonaws.com
      
      # - name: Install dependencies
      #   run: npm install --force

      # - name: Start application
      #   run: npm run start:dev
      #   timeout-minutes: 1

      
